const getMiniappOutputPath = require('./getMiniappOutputPath');
const getWebOutputPath = require('./getWebOutputPath');
const logWebpackConfig = require('./logWebpackConfig');
const getFilePath = require('./getFilePath');

module.exports = {
  getMiniappOutputPath,
  getWebOutputPath,
  logWebpackConfig,
  getFilePath
};
