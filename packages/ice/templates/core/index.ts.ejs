import {
  Link,
  Outlet,
  useParams,
  useSearchParams,
  useLocation,
  useNavigate,
} from '@ice/runtime/router';
<% if (globalStyle) {-%>
import '<%= globalStyle %>'
<% } -%>

export {
  Link,
  Outlet,
  useParams,
  useSearchParams,
  useLocation,
};

export {
  defineAppConfig,
  useAppData,
  useData,
  useConfig,
  Meta,
  Title,
  Links,
  Scripts,
  Data,
  Main,
  history,
} from '@ice/runtime';

<%- framework.imports %>

<% if (framework.exports) { -%>
export {
  <%- framework.exports %>
};
<% } -%>

export * from './types';
