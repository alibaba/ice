<% if (globalStyle) {-%>
import '<%= globalStyle %>'
<% } -%>
import type { PageConfig } from './types';
<%- framework.imports %>

<%- framework.variablesStr %>
<%- framework.dynamicImport %>

function definePageConfig(pageConfig: () => PageConfig): () => PageConfig {
  return pageConfig;
}

export {
  definePageConfig,
  <% if (framework.exports) { -%><%- framework.exports %><% } -%>
  <% if (framework.dynamicExports) { -%><%- framework.dynamicExports %><% } -%>
};

export * from './types';
