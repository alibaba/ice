import {
  defineAppConfig,
  useAppContext,
  Link,
  Outlet,
  Meta,
  Title,
  Links,
  Scripts,
  Main,
} from '@ice/runtime';
<%- framework.imports %>

<% if (framework.exports) { %>
  export {
    defineAppConfig,
    useAppContext,
    Link,
    Outlet,
    Meta,
    Title,
    Links,
    Scripts,
    Main,
    <%- framework.exports %>
  }
<% } %>

export * from './types';