import {
  useAppContext,
  Link,
  Outlet,
  Meta,
  Title,
  Links,
  Scripts,
  Root,
  defineAppConfig,
} from '@ice/runtime';
<%- framework.imports %>

<% if (framework.exports) { %>
  export {
    useAppContext,
    Link,
    Outlet,
    Meta,
    Title,
    Links,
    Scripts,
    Root,
    defineAppConfig,
    <%- framework.exports %>
  }
<% } %>

export * from './types';