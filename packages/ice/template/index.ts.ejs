import {
  defineAppConfig,
  useAppContext,
  Link,
  Outlet,
  Meta,
  Title,
  Links,
  Scripts,
  Root,
} from '@ice/runtime';
<%- framework.imports %>

<% if (framework.exports) { %>
  export {
    defineAppConfig,
    useAppContext,
    Link,
    Outlet,
    Meta,
    Title,
    Links,
    Scripts,
    Root,
    <%- framework.exports %>
  }
<% } %>

export * from './types';