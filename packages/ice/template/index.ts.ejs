import { useAppContext, Link, Outlet, defineAppConfig } from '@ice/runtime';
<%- framework.imports %>

<% if (framework.exports) { %>
  export {
    useAppContext,
    Link,
    Outlet,
    defineAppConfig,
    <%- framework.exports %>
  }
<% } %>

export * from './types';