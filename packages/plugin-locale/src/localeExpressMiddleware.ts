import { getLocaleData } from './getLocaleData';

export function localeExpressMiddleware(req, res, next) {
  // const localeData = getLocaleData();
  // const { redirectUrl } = localeData;

  // if (redirectUrl) {
  //   res.redirect(307, redirectUrl);
  // } else {
  //   next();
  // }
}
