import { withRouter } from 'rax-use-router';
import { createAppShared } from '@ice/runtime';

const {
  history,
  getHistory,
  setHistory
} = createAppShared;

// router api
export {
  withRouter,
  history,
  getHistory,
  setHistory,
};
