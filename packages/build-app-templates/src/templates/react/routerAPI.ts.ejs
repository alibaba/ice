import { withRouter } from '@ice/runtime/lib/reactRouter';
import { createAppShared } from '@ice/runtime/lib/createAppShared';

const {
  history,
  getHistory,
  setHistory,
} = createAppShared;

// router api
export {
  withRouter,
  history,
  getHistory,
  setHistory,
};
