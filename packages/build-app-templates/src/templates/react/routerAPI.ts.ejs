import { withRouter } from 'react-router';
import { createAppShared } from '@ice/runtime';

const {
  history,
  getHistory,
  setHistory,
} = createAppShared;

// router api
export {
  withRouter,
  history,
  getHistory,
  setHistory,
};
