import { withRouter } from '@ice/runtime/reactRouter';
import { createAppShared } from '@ice/runtime/createAppShared';

const {
  history,
  getHistory,
  setHistory,
} = createAppShared;

// router api
export {
  withRouter,
  history,
  getHistory,
  setHistory,
};
