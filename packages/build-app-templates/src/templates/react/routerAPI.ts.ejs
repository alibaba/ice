import { withRouter } from 'react-router';
import { createAppShared } from '@ice/runtime/lib/createAppShared';

const {
  history,
  getHistory,
  setHistory,
} = createAppShared;

// router api
export {
  withRouter,
  history,
  getHistory,
  setHistory,
};
