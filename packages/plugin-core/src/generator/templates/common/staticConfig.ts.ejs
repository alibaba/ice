let staticConfig = {};

try {
  <% if (staticConfig) {%>
    import '../../<%= staticConfig %>'<% }
  %>
} catch (error) {
  // ignore error
}

export default staticConfig.__esModule ? staticConfig.default : staticConfig;
