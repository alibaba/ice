import raxRenderer from 'renderer-rax';
import { createApp, emitLifeCycles, createHistory, pathRedirect, getHistory, staticConfig } from './common';
<% if (globalStyle) {%>import '../<%= globalStyle %>'<% } %>

export function runApp(appConfig) {
  raxRenderer({
    appConfig,
    createApp,
    emitLifeCycles,
    createHistory,
    pathRedirect,
    getHistory,
    staticConfig
  })
}
