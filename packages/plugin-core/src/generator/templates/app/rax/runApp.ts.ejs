import raxRenderer from 'renderer-rax';
import { createBaseApp, emitLifeCycles, createHistory, pathRedirect, getHistory, staticConfig } from './common';
<% if (globalStyle) {%>import '../<%= globalStyle %>'<% } %>

export function runApp(appConfig) {
  raxRenderer({
    appConfig,
    createBaseApp,
    emitLifeCycles,
    createHistory,
    pathRedirect,
    getHistory,
    staticConfig
  })
}
