// import * as ipc from './lib/ipc';

class AppBootHook {
  async didReady() {
    // Start ipc server
    // Ipc service for inter-process communication
    // ipc.init();
  }
}

export default AppBootHook;
