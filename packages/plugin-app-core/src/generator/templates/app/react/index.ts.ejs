export { runApp, runApp as createApp } from './runApp';
export * from './common';
export { lazy } from './lazy';
export * from './types';
export const APP_MODE = (global as any).__app_mode__ || process.env.APP_MODE;

<%- imports %>

<% if (exports) { %>
  export {
    <%- exports %>
  }
<% } %>
