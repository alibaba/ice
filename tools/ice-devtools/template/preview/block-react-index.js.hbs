import React from '{{blockNodeModulesPath}}react';
import ReactDOM from '{{blockNodeModulesPath}}react-dom'
import Block from '{{entry}}';

const mountNode = document.querySelector('#mountNode');
ReactDOM.render(
  <Block />,
  mountNode
);
